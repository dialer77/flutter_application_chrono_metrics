enum ReactionTestType {
  visual,
  auditory,
}

enum TestState {
  idle,
  ready,
  testing,
  finished,
}

enum AppTestType {
  reaction,
  timeGeneration,
  timeEstimationVisual,
  timeEstimationAuditory,
}

enum TestMode {
  practice,
  test,
}
